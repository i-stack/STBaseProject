---
description: auto-code-review (from skills-engineering)
alwaysApply: true
---

<!-- last-verified: 2026-07 -->
# 自动代码审查（Auto Code Review）

> **真值来源**：本文件为唯一详规正文。`auto-code-review/SKILL.md` 为入口；各端完整副本由 `scripts/sync-skills.sh` 同步；Cursor 项目内由 `sync-agent-preamble.sh` 生成 `.cursor/rules/auto-code-review.mdc`。

## 定位

auto-code-review 解决 AI 辅助编码的第 3 类失败模式：**代码听起来对，但实现有坑**。AI 生成代码后，同一模型无法发现自己的实现盲区——必须靠**跨模型审查**发现安全漏洞、竞态条件、边界遗漏等问题。

本 skill 是 `cross-model-review` 的姊妹篇：cross-model-review 审查计划（PLAN.md），auto-code-review 审查代码实现。

## 前置

- 主 agent 已完成代码修改（文件创建/编辑）。
- 当前环境至少有一个 reviewer CLI 可用（codex/gemini/claude）。无可用 CLI 时进入单模型降级模式。

## ACR-001 自动触发

主 agent 生成代码修改后，**必须**自动执行以下检测流程：

```bash
# 1. 检测是否有代码变更
git diff --name-only HEAD 2>/dev/null | grep -v '\.md$' | grep -q . || exit 0

# 2. 检测可用 reviewer CLI
bash skills-engineering/scripts/detect-review-clis.sh 2>/dev/null || \
  echo '{"clis":[],"available_count":0}'
```

**触发条件**：
- 回复中包含代码修改（非 .md 文件）
- 非 trivial 改动（排除：单行注释、格式化、typo 修复）
- 有可用 reviewer CLI（或单模型降级模式未禁用）

**跳过条件**：
- 纯文档更新（只有 .md 文件变更）
- trivial 改动（< 5 行非空白变更）
- 用户明确"不用审查" / "直接实施"
- 无可用 reviewer CLI 且 `AUTO_REVIEW_ALLOW_SELF_REVIEW=false`

## ACR-002 审查范围构造

主 agent 在调用 reviewer 前，必须构造审查输入：

```bash
# 获取变更文件列表
CHANGED_FILES=$(git diff --name-only HEAD)

# 获取 diff
DIFF=$(git diff HEAD)

# 构造审查上下文（每个变更文件的前 50 行）
for f in $CHANGED_FILES; do
  echo "=== $f ==="
  head -50 "$f" 2>/dev/null
done
```

审查输入包含：
1. **diff**：本次变更的完整 diff
2. **变更文件列表**：供 reviewer 定位上下文
3. **变更摘要**：主 agent 用一句话描述本次变更目的

## ACR-003 reviewer 只读调用

### 审查 prompt 模板

```
You are an adversarial code reviewer. Be skeptical and specific — your job is to find what breaks in the implementation, not to be agreeable.

## Change Summary
{CHANGE_SUMMARY}

## Diff
```diff
{DIFF}
```

## Changed Files Context
{FILES_CONTEXT}

Review the code changes for:
1. **Security vulnerabilities**: injection, auth bypass, data exposure, unsafe deserialization
2. **Race conditions**: concurrent access, missing locks, actor isolation violations
3. **Edge cases**: null/nil, empty collections, boundary values, error paths
4. **Error handling**: missing try/catch, silent failures, incorrect error propagation
5. **Performance**: O(n²) loops, unnecessary allocations, missing caching
6. **Correctness**: logic errors, off-by-one, wrong assumptions about data types

For each issue found:
- Severity: CRITICAL / HIGH / MEDIUM / LOW
- Location: file:line
- Problem: one-line description
- Fix: concrete one-line fix suggestion

End your reply with EXACTLY one line:
- `VERDICT: APPROVED` if no CRITICAL or HIGH issues found
- `VERDICT: REVISE` if any CRITICAL or HIGH issues exist

Do NOT modify any files. You are read-only.
```

### CLI adapter 调用命令

复用 `cross-model-review` 的 adapter 架构。每个 reviewer 必须以只读模式运行。

#### Codex adapter

```bash
# 构造审查输入文件
REVIEW_INPUT_FILE=".plan-reviews/${REVIEW_SLUG}/raw/review-input.md"
mkdir -p ".plan-reviews/${REVIEW_SLUG}/raw"
cat > "$REVIEW_INPUT_FILE" << EOF
## Change Summary
${CHANGE_SUMMARY}

## Diff
\`\`\`diff
${DIFF}
\`\`\`

## Changed Files Context
${FILES_CONTEXT}
EOF

# 调用 codex 审查
codex exec -s read-only --json \
  -o ".plan-reviews/${REVIEW_SLUG}/raw/codex-round${ROUND}.json" \
  "$(cat "$REVIEW_INPUT_FILE")

${REVIEW_PROMPT_BODY}" \
  < /dev/null 2>/dev/null

# 读取 verdict
grep -o 'VERDICT: \(APPROVED\|REVISE\)' ".plan-reviews/${REVIEW_SLUG}/raw/codex-round${ROUND}.json" | tail -1
```

**关键**：`< /dev/null` 强制必需——`codex exec` 在非交互式下会读 stdin，不重定向会永久 hang。

#### Gemini adapter

```bash
gemini -p "${REVIEW_PROMPT}" --approval-mode plan -o json --skip-trust \
  > ".plan-reviews/${REVIEW_SLUG}/raw/gemini-round${ROUND}.json"

# 读取 verdict
grep -o 'VERDICT: \(APPROVED\|REVISE\)' ".plan-reviews/${REVIEW_SLUG}/raw/gemini-round${ROUND}.json" | tail -1
```

#### Claude adapter

```bash
claude -p "${REVIEW_PROMPT}" --permission-mode plan --output-format json \
  > ".plan-reviews/${REVIEW_SLUG}/raw/claude-round${ROUND}.json"

# 读取 verdict
grep -o 'VERDICT: \(APPROVED\|REVISE\)' ".plan-reviews/${REVIEW_SLUG}/raw/claude-round${ROUND}.json" | tail -1
```

### 第一版不 pin model

每个 adapter 使用 CLI 配置里的**默认模型**。只有用户显式指定时才传 `--model`。

## ACR-004 主 agent 仲裁与修复

每轮 reviewer 返回后：

1. **读取审查结果**：从 `.plan-reviews/${REVIEW_SLUG}/raw/<reviewer>-round${ROUND}.json` 提取 verdict 和具体问题。

2. **仲裁纪律**：
   - **采纳有证据的批评**：问题具体到 file:line，且有明确修复建议 → 立即修复。
   - **拒绝不成立的批评**：reviewer 误读代码或提出不合理要求 → 记录拒绝理由。
   - **不盲从**（否则失去仲裁价值），**不无视**（否则失去审查价值）。

3. **修复代码**：主 agent 根据采纳的审查意见修改代码。

4. **记录仲裁**：追加到 `.plan-reviews/${REVIEW_SLUG}/REVIEW-LOG.md`：
   ```markdown
   ## Round ${ROUND} - ${reviewer}
   
   ### Reviewer Output
   ${REVIEWER_CRITIQUE}
   
   VERDICT: ${VERDICT}
   
   ### Orchestrator Response
   **Accepted:**
   - ${issue_1}: ${fix_description}
   
   **Rejected:**
   - ${issue_2}: because ${reason}
   ```

5. **进入下一轮**：修复完成后，再次调用 reviewer 审查。

## ACR-005 MAX_ROUNDS 与 deadlock

| 参数 | 默认 | 含义 |
|---|---|---|
| `MAX_ROUNDS` | `3` | 代码审查硬上限 |
| `REVIEW_SLUG` | `<date>-<slug>` | 归档目录名 |
| `REVIEW_DIR` | `.plan-reviews/${REVIEW_SLUG}` | 归档根目录 |

### 收敛条件

- **全部** reviewer 返回 `VERDICT: APPROVED` → 进入归档（ACR-006）。
- **任一** reviewer 返回 `VERDICT: REVISE` 且轮次 < MAX_ROUNDS → 仲裁并修复，进入下一轮。
- 轮次 = MAX_ROUNDS 且仍有 REVISE → 输出 deadlock 报告。

### deadlock 报告

```markdown
# Auto Code Review Deadlock

## Summary
经过 ${MAX_ROUNDS} 轮审查，以下问题仍未解决：

## Unresolved Issues

### Issue 1: ${title}
- **Severity**: ${severity}
- **Location**: ${file:line}
- **Reviewer's position**: ${reviewer_critique}
- **Orchestrator's counter**: ${main_agent_response}

## Recommendation
建议人工审查以上未决点后决定是否合并。
```

**禁止假装 approved**。未收敛时如实标记，交用户裁决。

## ACR-006 自动归档

审查完成后（无论 APPROVED 或 deadlock），**必须**自动归档到 `.plan-reviews/`。

### 归档目录结构

```
.plan-reviews/<date>-<slug>/
├── QUESTION.md           # 用户原始问题
├── RESPONSE.md           # AI 代码回复摘要（变更目的 + 文件列表）
├── REVIEW-LOG.md         # 审查日志（每轮 reviewer 输出 + 主 agent 仲裁）
├── diff.patch            # 本次变更的完整 diff
└── raw/                  # reviewer 原始输出
    ├── review-input.md   # 审查输入上下文
    ├── <reviewer>-round1.json
    └── <reviewer>-round2.json
```

### 归档流程

```bash
REVIEW_SLUG="$(date +%Y-%m-%d)-${SLUG}"
REVIEW_DIR=".plan-reviews/${REVIEW_SLUG}"
mkdir -p "${REVIEW_DIR}/raw"

# 确保 .gitignore 包含 .plan-reviews/
grep -qxF ".plan-reviews/" .gitignore 2>/dev/null || printf "\n.plan-reviews/\n" >> .gitignore

# 保存用户问题
cat > "${REVIEW_DIR}/QUESTION.md" << EOF
# 用户问题

${USER_QUESTION}
EOF

# 保存回复摘要
cat > "${REVIEW_DIR}/RESPONSE.md" << EOF
# 代码回复摘要

## 变更目的
${CHANGE_SUMMARY}

## 变更文件
${CHANGED_FILES}
EOF

# 保存 diff
git diff HEAD > "${REVIEW_DIR}/diff.patch"

# REVIEW-LOG.md 和 raw/ 已在审查过程中生成
```

### slug 生成规则

主 agent 根据用户问题自动生成 slug：
- 从问题中提取关键词（2-4 个词）
- 转换为 kebab-case（如 `login-rate-limit`）
- 日期前缀：`$(date +%Y-%m-%d)-<slug>`

### 归档原则

- **按项目保存**：归档到当前项目根的 `.plan-reviews/`，不进 skill 仓库。
- **默认忽略**：`.plan-reviews/` 默认加入 `.gitignore`。
- **强制归档**：无论审查结果如何（APPROVED 或 deadlock），都必须归档。
- **目的**：知识沉淀，相似问题回查。

## ACR-007 可配置 reviewer

### 默认选择策略

主 agent 根据宿主环境自动选择 reviewer：

| 主 agent 宿主 | 默认 reviewer 组合 |
|---|---|
| Claude Code | codex → gemini（避开 Anthropic） |
| Codex CLI | gemini → claude（避开 OpenAI） |
| Cursor / 其他 | codex → gemini → claude（按可用性排序） |

### 用户覆盖

用户可通过以下方式指定 reviewer：

1. **环境变量**：
   ```bash
   AUTO_REVIEW_REVIEWER=codex    # 只使用 codex
   AUTO_REVIEW_REVIEWERS=codex,gemini  # 使用 codex 和 gemini
   ```

2. **参数传递**：
   ```
   用户说："用 gemini 审查一下"
   → 主 agent 设置 reviewer=gemini
   ```

3. **配置文件**（可选）：
   ```json
   // .auto-review-config.json
   {
     "reviewers": ["codex", "gemini"],
     "max_rounds": 3,
     "allow_self_review": false
   }
   ```

### 多 reviewer 并行

当配置多个 reviewer 时，每轮**并行调用**所有 reviewer，收集全部 verdict 后统一仲裁。

```bash
# 并行调用 codex 和 gemini
codex exec ... &
CODEX_PID=$!
gemini -p ... &
GEMINI_PID=$!
wait $CODEX_PID $GEMINI_PID
```

## ACR-008 单模型降级

### 触发条件

- 可用 reviewer CLI 数量 = 1
- `AUTO_REVIEW_ALLOW_SELF_REVIEW` 未设置为 `false`

### 降级策略

使用唯一可用的 CLI，但切换为**对抗式审查 prompt**：

```
You are reviewing your own code with maximum skepticism. Assume every line has a bug. 
Find security vulnerabilities, race conditions, edge cases, and logic errors.
Be as critical as possible — your goal is to find what breaks.

[... same review criteria as ACR-003 ...]

IMPORTANT: You are reviewing code you wrote. Be extra skeptical of your own assumptions.
```

### 降级标注

在 REVIEW-LOG.md 顶部强制添加：

```markdown
> **WARNING**: 单模型自审模式。reviewer 与主 agent 使用同一模型，审查可信度降低。
> 建议安装其他 reviewer CLI（codex/gemini/claude）以获得跨模型审查。
```

### 禁用降级

用户可通过以下方式禁用单模型自审：

```bash
AUTO_REVIEW_ALLOW_SELF_REVIEW=false
```

禁用后，无可用 reviewer 时静默跳过审查，不输出任何审查相关内容。

## 安全规则

1. **reviewer 每轮只读**——codex `-s read-only`；gemini `--approval-mode plan`；claude `--permission-mode plan`。reviewer 永远不写文件。
2. **`< /dev/null` 必需**（codex）——非交互式下 stdin 不重定向会永久 hang。
3. **禁止 `/tmp` reviewer 缓冲**——reviewer 原始输出必须保存在 `.plan-reviews/${REVIEW_SLUG}/raw/`。
4. **timeout 600s 守卫**——每个 reviewer 调用加 10 分钟上限。超时视为失败，停止并告知用户。
5. **不 pin model**——用 CLI 默认模型，除非用户显式指定。
6. **循环必在 MAX_ROUNDS 终止**——硬上限，不无限循环。
7. **deadlock 不假装 approved**——未收敛时如实标记，交用户裁决。

## 审查质量自检

审查结束前过一遍：

- [ ] 每个 REVISE 是否都有 Accepted 或 Rejected 记录？
- [ ] Rejected 是否都写明理由？
- [ ] 是否全部 reviewer 都 APPROVED 后才标记为通过？
- [ ] reviewer raw 输出是否全部保存在 `.plan-reviews/` 下？
- [ ] REVIEW-LOG.md 是否完整保留所有轮次论证？
- [ ] deadlock 时是否如实标记，未假装 approved？
- [ ] 归档是否包含 QUESTION.md、RESPONSE.md、diff.patch？
- [ ] 单模型降级时是否添加了 WARNING 标注？

## 致谢

本 skill 复用 `cross-model-review` 的 CLI adapter 架构和 `detect-review-clis.sh` 探测脚本。审查 prompt 设计参考了 Google、OpenAI、Anthropic 的代码审查最佳实践。
