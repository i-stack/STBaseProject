# 库仓 CI：只做静态检查 + SPM 构建烟囱测试。
# 业务测试（466 单测 + UI 测试）在 i-stack/STBaseProjectExample 仓库的 tests.yml 中跑。

name: Swift

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      # 默认 xcode-select 常为旧版；脚本按 Info.plist 版本号选 /Applications 下最高的 Xcode（见 .github/select_xcode_ci.sh）。
      - name: Select Xcode
        run: bash "${GITHUB_WORKSPACE}/.github/select_xcode_ci.sh"

      - name: Check try? policy
        run: bash .github/check_try_question_mark.sh

      - name: SwiftLint
        run: |
          if ! command -v swiftlint >/dev/null 2>&1; then
            brew install swiftlint
          fi
          swiftlint version
          # 不加 --strict：warning 不阻断 CI，仅 .swiftlint.yml 中明确为 error 级别的规则会失败构建。
          # 待存量 warning 清零后可改为 --strict 提升门槛。
          swiftlint --reporter github-actions-logging

      - name: Resolve SPM dependencies
        run: xcodebuild -resolvePackageDependencies -scheme STBaseProject-Package

      - name: Build (iOS Simulator)
        run: |
          xcodebuild \
            -scheme STBaseProject-Package \
            -destination 'generic/platform=iOS Simulator' \
            build

  # 库 main 分支有更新时，触发 STBaseProjectExample 仓重新跑全量测试。
  # 仅在 push 到 main 时触发，避免 PR 阶段把下游测试也拉满。
  # notify-example:
  #   needs: build
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Trigger STBaseProjectExample tests
  #       uses: peter-evans/repository-dispatch@v3
  #       with:
  #         # 需要在仓库 Settings → Secrets and variables → Actions 中配置 EXAMPLE_REPO_PAT
  #         # PAT 至少需要 repo 写权限，用于触发 i-stack/STBaseProjectExample 的 repository_dispatch
  #         token: ${{ secrets.EXAMPLE_REPO_PAT }}
  #         repository: i-stack/STBaseProjectExample
  #         event-type: stbaseproject-updated
  #         client-payload: '{"sha": "${{ github.sha }}", "ref": "${{ github.ref }}"}'
